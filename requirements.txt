# Single shared virtualenv for every service — simplest to set up and host on one box.
# (backend + notes-worker)
fastapi==0.112.0
uvicorn[standard]==0.30.5
jinja2==3.1.4
python-multipart==0.0.9
google-api-python-client==2.140.0
google-auth==2.33.0
google-auth-oauthlib==1.2.1
httpx==0.27.0

# (meet-worker)
playwright==1.46.0

# (transcription-worker)
faster-whisper==1.0.3

# (shared: common/, all services)
pymongo==4.8.0
redis==5.0.8
rq==1.16.2
